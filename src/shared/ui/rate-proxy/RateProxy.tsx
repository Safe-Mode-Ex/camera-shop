import type {PropsWithChildren} from 'react';
import './RateProxy.css';

function RateProxy({children}: PropsWithChildren) {
  return (
    <>
      {children}
    </>
  );
}
export default RateProxy;
