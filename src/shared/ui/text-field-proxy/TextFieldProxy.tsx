import type {PropsWithChildren} from 'react';
import './TextFieldProxy.css';

function TextFieldProxy({children}: PropsWithChildren) {
  return (
    <>
      {children}
    </>
  );
}

export default TextFieldProxy;
